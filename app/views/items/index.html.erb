<% @items = Item.all %>
  <% if @items.size > 0 %>
    <h1> Add Items </h1>
     <table class="table table-stripe"”>
       <thead>
            <tr>
              <th>Name</th>
              <th>Price</th>
              <th>ID</th>
           </tr>
        </thead>
        <tbody>
          <% @items.each do |item| %>
          <tr>
            <td><%= item.name %></td>
            <td><%= "$#{item.unit_price}" %></td>
            <td><%= item.id %></td>
            <td>
              <%= link_to 'Add to Basket', show_basket_path(item: item), :method => :post, :class => "btn btn-sx btn-success" %>
            </td>
          </tr>
           <% end %>
        </tbody>
    </table>
  <%else%>
    <div class= "row col-lg012">
      <p class="lead">No items at this time.</p>
    </div>
  <% end %>

<%= link_to "Back to baskets listing", baskets_path %>
